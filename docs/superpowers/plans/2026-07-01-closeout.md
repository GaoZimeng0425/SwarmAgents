# Closeout: Docs + Turbo + Verify (Phase 7) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bring the monorepo's documentation and task orchestration in line with the multi-platform structure landed in Plans 1–5: a root README that describes the three-platform architecture, per-app READMEs, turbo `outputs` covering every app's build artifacts, and a single root `verify` script.

**Architecture:** Documentation-only plus small turbo/script wiring. No source changes, no new features. Deliberately out of scope: the `submitGoal` path for the extension/RN (it needs a `ProviderInjection` the clients don't own — that's a future design, flagged in the README rather than half-built here).

**Tech Stack:** Markdown, turbo, biome, pnpm scripts.

**Spec:** `docs/superpowers/specs/2026-07-01-multiplatform-structure-design.md` (§10 Phase 7).

## Global Constraints

- **Language:** READMEs and comments in English. Conversation in Chinese.
- **No source changes:** this plan touches only `*.md`, `turbo.json`, and the root `package.json` scripts. No `src/`, no `@swarm/*` packages.
- **Scope discipline:** do NOT add provider/submitGoal support — document it as future (spec hasn't designed how the extension/RN obtain a `ProviderInjection`).
- **Verify bar:** `pnpm verify` (new root script) runs typecheck + lint + test + check-boundaries and must be green.
- **Worktree:** Execute in `worktree-closeout`. One commit per task.

---

## File Structure

**Create:**
- `apps/desktop/README.md` — desktop is the Electron host + WS host.
- `apps/extension/README.md` — WXT MV3 extension, connects to desktop.
- `apps/mobile/README.md` — Expo RN, connects to desktop.

**Modify:**
- `README.md` (root) — rewrite for the multi-platform monorepo.
- `turbo.json` — `outputs` covers extension/mobile build dirs.
- `package.json` (root) — add `verify` script.

---

## Task 1: Root README + turbo outputs + verify script

**Files:**
- Modify: `README.md`, `turbo.json`, `package.json`
- Test: `pnpm verify` green.

- [ ] **Step 1: Rewrite the root README**

Replace the entire contents of `README.md` with:

```markdown
# swarm-agents

A multi-platform monorepo: an Electron desktop app that owns the agent runtime,
plus a Chrome extension and a React Native client that drive it over a
loopback WebSocket.

```
apps/
├── desktop/     Electron — the agent-runtime host (main + service sidecar + renderer) + WS host
├── extension/   Chrome MV3 (WXT) — remote client over WS
└── mobile/      Expo React Native — remote client over WS
packages/
├── protocol/    @swarm/protocol — wire types + ServiceClient/ServiceTransport (zod-only)
└── shared/      @swarm/shared — platform-neutral pure logic (agents/constants/tokens)
tools/           shared tsconfig bases + check-boundaries.mjs
```

The extension and RN app cannot run the agent runtime (it needs native modules
+ child-process spawns), so both are **remote clients**: they connect to the
desktop app's loopback WS host and drive the same `ServiceClient` from
`@swarm/protocol`.

## Quick start

```bash
pnpm install
pnpm dev          # starts the desktop app (Electron)
```

The desktop opens a loopback WS server on `127.0.0.1:47777`; its port + token
are written to `~/Library/Application Support/SwarmAgents/ws-host.json`
(macOS) for the extension/RN to read.

## Per-app workflows

| App | Command | Notes |
|---|---|---|
| desktop | `pnpm --filter @swarm/desktop run dev` | Electron host |
| extension | `pnpm --filter @swarm/extension run build` | load `apps/extension/.output/chrome-mv3` unpacked in Chrome |
| mobile | `pnpm --filter @swarm/mobile run start` | Expo; press `i` for iOS Simulator |

For the extension/RN, paste the token from `ws-host.json` into their settings,
then "test connection" (lists agents from the desktop service).

## Scripts

- `pnpm verify` — typecheck + lint + test + package-boundary check.
- `pnpm build` / `pnpm test` / `pnpm typecheck` — turbo-fanned to every app.
- `pnpm check-boundaries` — enforce that `@swarm/protocol` and `@swarm/shared`
  import nothing platform-bound (no Node/electron/react/native).

## Status (v1)

- Desktop is the only agent-runtime host. Extension/RN are connection probes
  (`listAgents`) in v1 — `submitGoal` needs a `ProviderInjection` the clients
  don't own; exposing provider config over WS is future work.
- Single external WS peer at a time (the host rejects a second).

See `docs/superpowers/specs/2026-07-01-multiplatform-structure-design.md` for
the full design.
```

- [ ] **Step 2: turbo.json — cover every app's outputs**

Edit `turbo.json`. The `build.outputs` currently lists only `apps/desktop/out/**` + `apps/desktop/dist/**`. Add the extension's `.output/` (mobile has no turbo build output — Expo bundles on demand, not via `turbo run build`):

```json
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "apps/desktop/out/**",
        "apps/desktop/dist/**",
        "apps/extension/.output/**",
        "dist/**",
        "out/**"
      ]
    },
```

(Leave `test`, `typecheck`, `lint`, `dev`, `check-boundaries` as they are.)

- [ ] **Step 3: Root verify script**

Edit `package.json` `scripts` to add `verify` (insert after `typecheck`):

```json
    "verify": "pnpm run typecheck && pnpm run lint && pnpm run test && pnpm run check-boundaries",
```

- [ ] **Step 4: Run verify**

```bash
pnpm verify
```
Expected: typecheck + lint + test + check-boundaries all green (turbo fans test/typecheck across desktop/extension/mobile; boundary passes). If `pnpm test` skips the mobile/extension tests because of the `minimumReleaseAge` policy on `expo`, run `PNPM_CONFIG_MINIMUM_RELEASE_AGE=0 pnpm verify` — that's a main-checkout policy issue, not a code issue.

- [ ] **Step 5: Commit**

```bash
git add README.md turbo.json package.json
git commit -m "docs: root README + turbo outputs + verify script (Phase 7)"
```

---

## Task 2: Per-app READMEs

**Files:**
- Create: `apps/desktop/README.md`, `apps/extension/README.md`, `apps/mobile/README.md`
- Test: files exist; no verify change (docs only).

- [ ] **Step 1: apps/desktop/README.md**

```markdown
# @swarm/desktop

The Electron host. Owns the agent runtime (a Node `utilityProcess` sidecar under
`src/service/` — `better-sqlite3`, `sqlite-vec`, `claude-agent-sdk`) and exposes
it to remote clients over a loopback WebSocket.

## Run

```bash
pnpm --filter @swarm/desktop run dev     # electron-vite dev
pnpm --filter @swarm/desktop run build   # typecheck + electron-vite build
pnpm --filter @swarm/desktop run test    # vitest (through Electron's node)
```

## WS host

On boot the main process starts `ws.WebSocketServer` on `127.0.0.1:47777`
(`src/main/host/`). Port + a random token are persisted to
`userData/ws-host.json`; remote clients (extension/RN) authenticate with the
token as a `Sec-WebSocket-Protocol` subprotocol (`swarm.<token>`). v1 allows
one external peer at a time.

See the repo root README for the overall architecture.
```

- [ ] **Step 2: apps/extension/README.md**

```markdown
# @swarm/extension

Chrome MV3 extension (built with WXT). A remote client that connects to the
desktop's WS host and drives `@swarm/protocol`'s `ServiceClient` over a
browser-WebSocket transport.

## Build

```bash
pnpm --filter @swarm/extension run build    # → .output/chrome-mv3/
```

## Load

1. Start the desktop (`pnpm --filter @swarm/desktop run dev`).
2. `cat ~/Library/Application\ Support/SwarmAgents/ws-host.json` — copy the token.
3. Chrome → `chrome://extensions` → Developer mode → Load unpacked → select
   `apps/extension/.output/chrome-mv3`.
4. Extension Options → paste token → Save.
5. Popup → "test connection" → shows the desktop's agent count.

## Status

v1 is a connection probe (`listAgents`). `submitGoal` is deferred until provider
config is exposed over WS.
```

- [ ] **Step 3: apps/mobile/README.md**

```markdown
# @swarm/mobile

Expo React Native app (managed workflow). A remote client over the desktop's WS
host, same `ServiceClient`/transport shape as the extension.

## Run

```bash
pnpm --filter @swarm/mobile run start    # Expo; press i for iOS Simulator
pnpm --filter @swarm/mobile run test     # vitest (transport)
```

## Connect

1. Start the desktop (`pnpm --filter @swarm/desktop run dev`).
2. Copy the token from `~/Library/Application\ Support/SwarmAgents/ws-host.json`.
3. In the app: Settings → paste token → Save → reload → Home "test connection".

iOS Simulator's `127.0.0.1` reaches the Mac's desktop host directly. (Android
emulator would need `10.0.2.2`.)

## Status

v1 is a connection probe (`listAgents`). `submitGoal` is deferred until provider
config is exposed over WS.
```

- [ ] **Step 4: Commit**

```bash
git add apps/desktop/README.md apps/extension/README.md apps/mobile/README.md
git commit -m "docs: per-app READMEs (desktop/extension/mobile)"
```

---

## Acceptance

Plan 6 is complete when **all** hold:

1. Root `README.md` describes the three-platform architecture + how to run each app.
2. `apps/{desktop,extension,mobile}/README.md` each exist and are accurate.
3. `turbo.json` build `outputs` includes the extension's `.output/`.
4. `pnpm verify` (root) runs typecheck + lint + test + check-boundaries and is green.
5. No source changes (only `*.md`, `turbo.json`, root `package.json`).

This is the final plan of the multi-platform migration (Plans 1–6).
